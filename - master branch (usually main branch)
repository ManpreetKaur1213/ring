- master branch (usually main branch)

- git branch "<branch name>"

- git push --set-upstream origin "<branch name>"

- git merge "origin/<branch name>" "origin/<master branch>"

- git merge pagination origin/master

- orgin/master will be ahead of local master

Github													| 												Manpreet macbook air

- oring/master																- master
- master + 1																	- master + 1 or git pull 
- master + new code 			<---------					- master + new code (git push)

- origin/pagination														- pagination (git push --set-upstream origin master)
- pagination 																	- pagination + new code


Pull Request
- compare changes between main branch (usually master) and new branch (example: pagination)

- master 					<--- merge it in 						- pagination









---------------------------


total_results = 315
display = 20
pages = x
x * 20 = 315 using Ceil function